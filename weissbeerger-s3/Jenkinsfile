pipeline{
    agent any
    stages{
        stage('Terraform'){
            steps{
                withAWS(){
                    sh 'terraform init'
                    sh 'terraform apply -f -var-file="${vars_file}"'
                }
            }
        }
    }
}